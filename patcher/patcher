#!/usr/bin/env python3
"""
OpenClaw Patcher - Unified patch management system

Thin wrapper that invokes the openclaw_patcher package.
"""

import sys
from pathlib import Path

# Add patcher directory to Python path
patcher_dir = Path(__file__).parent
sys.path.insert(0, str(patcher_dir))

from openclaw_patcher.cli import main

if __name__ == "__main__":
    sys.exit(main())
